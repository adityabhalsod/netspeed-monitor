package com.netspeed.monitor.di

// NativeSpeedBridge is annotated with @Singleton and @Inject constructor, so Hilt
// can inject it directly without an explicit @Provides method in a module.
// This file is retained as a placeholder for future native-layer bindings.

