package com.netspeed.monitor.presentation.theme

import androidx.compose.ui.graphics.Color

// === Modern dark theme palette for network speed monitor ===

// Primary brand accent: vivid cyan for interactive elements
val AccentCyan = Color(0xFF00D4FF)
// Secondary brand accent: electric blue for highlights
val AccentBlue = Color(0xFF3366FF)
// Standard blue for light mode primary
val PrimaryBlue = Color(0xFF1976D2)

// Speed indicator colors: neon green for download, amber for upload
val DownloadGreen = Color(0xFF00E676)
val UploadOrange = Color(0xFFFF9100)

// Dark theme surface hierarchy
val DarkBackground = Color(0xFF0A0E21)
val DarkSurface = Color(0xFF131729)
val DarkSurfaceVariant = Color(0xFF1A1F3D)

// Dark theme text colors
val TextPrimary = Color(0xFFF0F0FF)
val TextSecondary = Color(0xFF7B8CAD)

// Light theme surface hierarchy
val LightBackground = Color(0xFFF5F7FF)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFE8EDF5)

// Light theme text colors
val LightTextPrimary = Color(0xFF0A0E21)
val LightTextSecondary = Color(0xFF5A6488)
